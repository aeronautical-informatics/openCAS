[package]
name = "cas-rs"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
csv = "1"
quote = "1"
nalgebra = { version = "0.30", default-features = false, features = ["macros"] }

